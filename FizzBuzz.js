// let a = true;
// const output = typeof a;
// console.log(output);

function compare(a) {
  return typeof a === "number" ? "A is a Number" : "A is not a Number";
}

const output = compare(10);
console.log(output);

//Typeof concept is important for Codeing Practice
