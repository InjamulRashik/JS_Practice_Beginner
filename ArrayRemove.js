const numbers = [1, 2, 3, 4];

// // Remove from End
// numbers.pop(argument / value);

// // Remove from Start
// numbers.shift(argument / value);

// // Remove from Middle
// numbers.splice(indexofElement,numberOfElemetnsToDelete);
